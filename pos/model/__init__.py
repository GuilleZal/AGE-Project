# pos/model — Domain model layer
# Dataclasses, enums, exceptions, and database connection manager.
# No business logic here — that lives in pos/service/.
# No SQL queries here — that lives in pos/repository/.
